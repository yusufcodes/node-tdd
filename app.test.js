const request = require("supertest");
const app = require("./app");

describe("To Tods API", () => {
  it("GET /todos -> array of TODOs", () => {});

  it("GET /todos/id -> a single TODO by ID", () => {});

  it("GET /todos/id -> 404 if not found", () => {});

  it("POST /todos -> create new todo", () => {});

  it("POST /todos -> create new todo", () => {});
});
